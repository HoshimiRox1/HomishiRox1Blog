# AGENTS.md

> 本文件定义 Roxy Blog 的项目事实、开发边界、文档同步规则和验收方式。

## 1. 项目简介

Roxy Blog 是一个以“帅、酷炫、强视觉动效”为第一印象的个人网站。当前视觉母风格是“清爽新粗野主义 + 动漫角色舞台 + 海报式个人站”：冷白底、粗黑边、超大标题、密网格、pastel 色带、HUD 卡片、角色舞台、底部滚动文字和明确的分镜式动效。Phase 1 聚焦主页面和全站骨架：Home 内滚轮角色切换、右侧 FEED 式竖向色带手风琴，以及 Profile / Portfolio / Blog 占位页。

## 2. 项目目录结构

```txt
Roxy-blog/
├─ AGENTS.md
├─ docs/
│  ├─ brief.md
│  ├─ DESIGN.md
│  ├─ Technical-Stack.md
│  ├─ TODO.md
│  ├─ modules/
│  ├─ superpowers/
│  │  ├─ specs/
│  │  └─ plans/
│  └─ templates/
└─ waifus/
```

## 3. 技术栈

- 当前冻结：Vite + React + GSAP + React Router + 普通 CSS 或 CSS Modules。
- 当前阶段不引入数据库、后端、CMS、SSR 框架或大型 UI 框架。
- 未来 Blog 内容化阶段可迁移为 Astro 外壳 + React Islands，但 Phase 1 不引入 Astro。
- 如果询问具体库、框架、SDK、API、CLI 或云服务用法，先用 Context7 获取当前文档。

## 4. Phase 1 范围

- Home 首屏：大标题、小字内容、底部滚动文字，不展示角色。
- Home 滚轮后：标题缩小滑到左上角，角色从右侧手风琴区域滑入。
- 角色切换：一次滚轮手势只切换一个角色，动画期间锁定。
- 初始角色素材：`waifus/珂莱塔-cutout.png`、`waifus/洛琪希-cutout.png`。
- 右侧手风琴：Home、Profile、Portfolio、Blog 四条竖向色带并排，使用 `docs/DESIGN.md` 定义的 pastel 页面识别色。
- 点击色带后，色带横向展开铺满屏幕，再进入对应占位页。
- BGM 播放器固定左下角；Home 页面时位于底部滚动文字上方。

## 5. 后续待完善

- Portfolio：作品滚动擦除切换。
- Profile：个人档案卡、喜好、联系方式、GitHub。
- Blog：博客笔记、番剧记录、电影记录、游戏记录四个板块。
- 角色系统：补齐 5 个动漫角色 IP、顺序、标题和配色。
- BGM：曲目列表、播放策略、音量、状态持久化。

## 6. 文档规则

- `docs/brief.md` 是产品边界事实来源。
- `docs/DESIGN.md` 是全站设计风格事实来源；涉及页面、组件、动效或视觉调整时必须先阅读并遵守。
- `docs/Technical-Stack.md` 是技术选型事实来源，确认后不当开发日志反复改写。
- `docs/TODO.md` 是当前阶段和下一步任务事实来源。
- 功能模块文档写入 `docs/modules/`，按功能责任划分，不按前端/后端强行拆分。
- 每个 spec 或 slice 验收后，更新 `docs/TODO.md` 和相关模块文档。

## 7. 开发规则

- 开始任务前先阅读 `AGENTS.md`、`docs/TODO.md` 和相关模块文档。
- 任何页面开发、组件开发、动效开发或视觉样式修改前，必须先阅读 `docs/DESIGN.md`，用它确认设计风格和可复用元素，再开始方案或代码；不要在不了解设计系统的情况下盲目开发。
- Profile / Portfolio / Blog 后续开发需要继承 Home 的视觉语法，但根据页面内容形成各自版式人格；不要简单复制 Home，也不要做成与全站风格割裂的普通模板页。
- 修改代码前先用 `rg` 搜索相关入口、接口、配置项、测试和已有实现。
- 不做无关重构，不顺手改目录结构、命名风格或历史格式。
- 写代码时模块文件开头保留段级中文注释，函数保留单行中文注释。
- 新增大型依赖、框架、数据库、云服务、模型服务前，先说明原因和影响并得到确认。
- 不删除看似无用的文件，除非确认它不被项目使用且用户同意。

## 8. 安全规则

- 真实密钥、Token、账号密码只能写入 `.env`，不得提交。
- 新增环境变量时，同步更新 `.env.example` 和 `docs/Security.md`。
- 不在日志、错误信息、截图、测试快照或示例输出中暴露密钥。
- 不把角色素材或用户真实数据上传到第三方服务，除非用户明确允许。

## 9. 验收规则

- 修改完成后，按改动范围运行构建、类型检查、lint 或手动浏览器验证。
- 最终回复需要说明改了什么、关键文件、验证命令、未验证项和剩余风险。
- 对动效任务，不能只以代码存在作为完成标准，必须验证可见运动和布局遮挡。
